import styled from 'styled-components';

const MakeupToolsWrapper = styled.ol`
  margin-top: 25px;
`;

const ImgContainer = styled.div`
  margin-top: 15px;
  display: flex;
  justify-content: center;
`;

export { MakeupToolsWrapper, ImgContainer };
